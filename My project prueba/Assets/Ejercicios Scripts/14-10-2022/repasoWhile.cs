using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class repasoWhile : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
        int i = 0;

        do
        {
            i++;
        } while (i < 100);
    }

    // Update is called once per frame
    private void Update()
    {
    }
}